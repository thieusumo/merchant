 <ul class="list-inline col-sm-12 listservices" style="width: 100%;margin: 0px;padding-left: 20px">
   {{--  @if($list_service != "")
        @foreach($list_service as $service)
         <li class="list-inline-item block-service service {{($service->promotion_discount!='')?"promotion":""}}" value="{{$service->service_id}}">
             <a href="#">
                 <span>{{$service->service_name}}</span>
                 <span class="sprice">${{$service->service_price}}</span>
             </a>         
         </li>
        @endforeach
    @endif --}}
</ul>